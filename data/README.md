# Data Directory

Place the raw dataset files for the UCI Diabetes 130-US Hospitals dataset in `data/raw/`.

Required files:

- `diabetic_data.csv`
- `IDS_mapping.csv`

The `data/raw/` directory is intentionally ignored by git so raw data stays local.

